---
phase: 04-community-features-future
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - src/components/layout/Header.tsx
  - src/i18n/en.json
  - src/i18n/zh.json
autonomous: false
requirements:
  - D-01
  - D-02
  - D-16
  - D-17

must_haves:
  truths:
    - "LoginButton appears in Header navigation next to locale toggle"
    - "Clicking sign-in redirects to GitHub OAuth flow"
    - "Clicking signed-in avatar shows sign-out option"
    - "All community i18n keys are present in both en.json and zh.json"
    - "Full community feature flow works end-to-end: login -> review -> vote -> submit -> admin review"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Header with LoginButton integrated in nav area"
    - path: "src/i18n/en.json"
      provides: "Complete community namespace with all keys"
    - path: "src/i18n/zh.json"
      provides: "Complete community namespace with all keys"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/components/auth/LoginButton.tsx"
      via: "Import and render in nav"
      pattern: "import.*LoginButton"
---

<objective>
Integrate LoginButton into the Header, verify all i18n keys are complete, and perform a final checkpoint to validate the entire community feature flow end-to-end.

Purpose: This is the integration plan that connects all community features together. The LoginButton in the header provides the universal entry point for authentication. The final checkpoint ensures everything works as a cohesive system.

Output: LoginButton visible in header, complete i18n coverage, and verified end-to-end community feature flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-community-features-future/04-CONTEXT.md
@.planning/phases/04-community-features-future/04-UI-SPEC.md
@.planning/phases/04-community-features-future/04-01-SUMMARY.md
@.planning/phases/04-community-features-future/04-02-SUMMARY.md
@.planning/phases/04-community-features-future/04-03-SUMMARY.md
@.planning/phases/04-community-features-future/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate LoginButton into Header and finalize i18n keys</name>
  <files>
    src/components/layout/Header.tsx
    src/i18n/en.json
    src/i18n/zh.json
  </files>
  <read_first>
    src/components/layout/Header.tsx
    src/components/auth/LoginButton.tsx
    src/i18n/en.json
    src/i18n/zh.json
  </read_first>
  <action>
**Step 1: Update src/components/layout/Header.tsx**

Add LoginButton import and render it in the nav area, before the locale toggle button:
```tsx
import LoginButton from '@/components/auth/LoginButton'
```

In the nav element, add LoginButton before the locale toggle. The LoginButton should appear between the "About" link and the locale toggle button. This matches UI-SPEC placement: "right side, before locale toggle".

**Step 2: Verify and finalize all i18n keys in en.json and zh.json**

Read both JSON files and ensure the "community" namespace contains ALL keys used across Plans 02, 03, 04. Specifically verify these keys exist:

Review keys (from Plan 02): community.reviews, community.writeReview, community.updateReview, community.noReviews, community.noReviewsBody, community.signInToReview, community.reviewSubmitted, community.reviewUpdated, community.reviewError, community.content, community.contentPlaceholder, community.platform, community.useCase, community.useCasePlaceholder, community.reviewsCount, community.loadMore

Tag keys (from Plan 03): community.tags, community.signInToVote, community.tagVoteLimit, community.votedCount

Submit keys (from Plan 04): community.submitTitle, community.submitDescription, community.submitConfirmation, community.submitValidation, community.submitDuplicate, community.submitNotFound, community.signInToSubmit, community.notes, community.notesPlaceholder, community.suggestedTags, community.repoUrl, community.repoUrlPlaceholder, community.submitTool

If any keys are missing, add them to both en.json and zh.json.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --passWithNoTests</automated>
  </verify>
  <done>
    - Header.tsx imports and renders LoginButton in nav area
    - LoginButton appears between About link and locale toggle
    - en.json community namespace has all 29+ keys matching zh.json
    - zh.json community namespace has all 29+ keys matching en.json
    - TypeScript compiles
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end community feature verification</name>
  <files>none</files>
  <action>Human verification of complete community feature flow</action>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>All community features verified working end-to-end</done>
  <what-built>
Complete community features system: GitHub OAuth authentication, user reviews with rating distribution, tag voting with 3-vote cap, tool submission with admin review workflow, LoginButton in header.
  </what-built>
  <how-to-verify>
1. **Auth flow**: Click "Sign in" button in header. Verify redirect to GitHub OAuth. After login, verify avatar + username appears in header.

2. **Review submission**: Navigate to any tool detail page. Click "Write a Review". Select 3-5 stars. Add optional content. Click submit. Verify review appears in list and avgRating updates.

3. **Review upsert**: Submit a second review for the same tool. Verify the existing review is updated (not duplicated).

4. **Tag voting**: On a tool detail page, click tag pills to vote. Verify max 3 votes enforced. Click a voted tag to unvote. Verify vote count updates.

5. **Tool submission**: Navigate to /submit page. Enter a valid GitHub repo URL. Verify validation shows green checkmark. Submit. Verify "pending review" confirmation appears.

6. **Admin review**: Navigate to /admin/submissions (while logged in as admin user). Verify pending submissions appear. Click Approve on one submission. Verify tool is created with GitHub metadata.

7. **Sign out**: Click avatar in header. Verify session ends and LoginButton returns to "Sign in" state.

8. **i18n**: Switch to Chinese locale (click "中文" in header). Verify all community feature text appears in Chinese.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header -> Auth | LoginButton triggers OAuth flow |
| All community features | Depend on authenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | I | LoginButton session display | mitigate | Session data from Auth.js JWT; no sensitive data exposed (only name + image) |
| T-04-19 | T | Session cookie theft | accept | Auth.js handles cookie security (HttpOnly, Secure, SameSite); HTTPS in production |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- LoginButton renders in Header between nav links and locale toggle
- en.json and zh.json have matching community namespace keys
- End-to-end flow verified by human: login -> review -> tag vote -> submit -> admin
</verification>

<success_criteria>
- LoginButton visible in header for all pages
- GitHub OAuth login/logout works
- Review submission and upsert verified
- Tag voting with 3-vote cap verified
- Tool submission with admin approval verified
- Bilingual text displays correctly in both locales
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-community-features-future/04-05-SUMMARY.md`
</output>
