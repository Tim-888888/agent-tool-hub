---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/page.tsx
  - src/app/tools/ToolsClient.tsx
  - src/app/tools/[slug]/page.tsx
  - src/app/tools/[slug]/ToolDetailClient.tsx
  - src/app/categories/[slug]/page.tsx
  - src/app/rankings/page.tsx
  - src/components/ui/FilterBar.tsx
autonomous: true
requirements:
  - REQ-05

must_haves:
  truths:
    - "Home page shows featured tools, newest tools, and categories from API data"
    - "Tools list page fetches and displays tools from /api/tools with working filters"
    - "Tool detail page fetches tool from /api/tools/[slug]"
    - "Category page fetches category and tools from /api/categories/[slug]"
    - "Rankings page shows tools sorted from API data"
    - "FilterBar dropdowns populate from API data (platforms and categories)"
    - "Mock data remains as fallback importable from mock-data.ts"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page fetching from API"
      contains: "fetch"
    - path: "src/app/tools/ToolsClient.tsx"
      provides: "Tools list with API-based filtering"
      contains: "/api/tools"
    - path: "src/app/tools/[slug]/page.tsx"
      provides: "Tool detail fetching from API"
      contains: "/api/tools"
    - path: "src/app/categories/[slug]/page.tsx"
      provides: "Category page fetching from API"
      contains: "/api/categories"
    - path: "src/app/rankings/page.tsx"
      provides: "Rankings page fetching from API"
      contains: "/api/tools"
    - path: "src/components/ui/FilterBar.tsx"
      provides: "Filter bar with API-fetched categories and platforms"
      contains: "fetch"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/tools/trending"
      via: "fetch in useEffect"
      pattern: "fetch.*api/tools"
    - from: "src/app/tools/ToolsClient.tsx"
      to: "/api/tools"
      via: "fetch with query params"
      pattern: "fetch.*api/tools"
    - from: "src/app/tools/[slug]/page.tsx"
      to: "/api/tools/[slug]"
      via: "fetch in server component"
      pattern: "fetch.*api/tools"
    - from: "src/components/ui/FilterBar.tsx"
      to: "/api/categories"
      via: "fetch categories and platforms for dropdowns"
      pattern: "fetch.*api/categories"
---

<objective>
Replace all mock-data imports with API fetch calls across all pages, so the frontend renders real database data.

Purpose: This is the user-visible outcome of the entire backend phase. Pages must work with real data from PostgreSQL via API routes.

Output: All 6 pages + FilterBar component updated to fetch from API routes instead of importing mock data directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md

<interfaces>
<!-- API response shapes the frontend must consume -->
From Plan 02 API routes:
```
GET /api/tools?page=1&limit=20&type=mcp&platform=cursor&category=database&q=search&sort=stars
-> { success: true, data: Tool[], meta: { total, page, limit, totalPages } }

GET /api/tools/[slug]
-> { success: true, data: Tool }  (with flattened categories[] and platforms[])

GET /api/tools/trending
-> { success: true, data: Tool[] }  (max 6)

GET /api/tools/newest
-> { success: true, data: Tool[] }  (max 6)

GET /api/categories
-> { success: true, data: CategoryWithCount[] }

GET /api/categories/[slug]
-> { success: true, data: { ...category, tools: Tool[] } }
```

Current import patterns to replace:
- `import { getFeaturedTools, getNewestTools, CATEGORIES, TOOLS, PLATFORMS } from "@/lib/mock-data"`
- `import { getTools } from "@/lib/mock-data"`
- `import { getToolBySlug } from "@/lib/mock-data"`
- `import { TOOLS } from "@/lib/mock-data"`
- `import { CATEGORIES, getTools } from "@/lib/mock-data"`
- `import { CATEGORIES as ALL_CATEGORIES, PLATFORMS as ALL_PLATFORMS } from '@/lib/mock-data'`
- `import { PLATFORMS } from '@/lib/mock-data'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update home page, tool detail page, category page, and rankings page to use API</name>
  <files>src/app/page.tsx, src/app/tools/[slug]/page.tsx, src/app/tools/[slug]/ToolDetailClient.tsx, src/app/categories/[slug]/page.tsx, src/app/rankings/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current: imports from mock-data, uses getFeaturedTools/getNewestTools/CATEGORIES/TOOLS/PLATFORMS)
    - src/app/tools/[slug]/page.tsx (current: imports getToolBySlug, TOOLS from mock-data)
    - src/app/tools/[slug]/ToolDetailClient.tsx (current: imports PLATFORMS from mock-data)
    - src/app/categories/[slug]/page.tsx (current: imports CATEGORIES, getTools from mock-data)
    - src/app/rankings/page.tsx (current: imports TOOLS from mock-data)
    - src/types/index.ts (Tool, Category types)
  </read_first>
  <action>
Update 5 page files to fetch data from API routes instead of importing from mock-data.

**1. `src/app/page.tsx`** — Home page (client component):
- Remove imports: `getFeaturedTools`, `getNewestTools`, `CATEGORIES`, `TOOLS`, `PLATFORMS` from mock-data
- Keep `useI18n` import
- Add `useState`, `useEffect` imports (already has useI18n)
- Add state: `featuredTools`, `newestTools`, `trendingTools`, `categories`, `platforms`, `stats`
- In useEffect, fetch:
  - `/api/tools?limit=6&sort=stars` for featured tools (or use a featured param if API supports it — check if isFeatured filter exists in buildWhereClause; if not, just sort by stars and take top 6)
  - `/api/tools/newest` for newest tools
  - `/api/tools/trending` for trending tools
  - `/api/categories` for categories
- Derive stats from fetched data (tools count from meta.total, categories length, etc.)
- Render with loading states (show nothing or skeleton until data loads)
- Keep the same JSX structure, just use state variables instead of direct mock data calls
- IMPORTANT: This is a "use client" component — all fetches happen in useEffect on the client side

**2. `src/app/tools/[slug]/page.tsx`** — Tool detail (server component):
- Remove imports: `TOOLS`, `getToolBySlug` from mock-data
- In the page function: `const res = await fetch(`${process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000'}/api/tools/${slug}`, { cache: 'no-store' }); const { data: tool } = await res.json();`
- IMPORTANT: Server components can fetch directly — no useEffect needed. Use absolute URL with process.env or relative URL if supported. For `generateStaticParams`, keep importing from mock-data temporarily (or fetch from API at build time — but that requires database at build time). For now, keep `generateStaticParams` using mock-data import since SSG needs data at build time. Only change the page rendering to use API.
- Actually, simpler approach: Keep `generateStaticParams` importing from mock-data (it only runs at build time). Change the page body to fetch from API. If the fetch fails or returns no data, call `notFound()`.
- Remove `getToolBySlug` import, replace with API fetch
- Keep `buildJsonLd` function (it operates on the tool object regardless of source)

**3. `src/app/tools/[slug]/ToolDetailClient.tsx`** — Tool detail client:
- Remove import: `PLATFORMS` from mock-data
- Instead, receive `platforms` as a prop alongside `tool`, OR derive platforms from `tool.platforms` in the Tool object (which already contains platform data from the API response)
- Check: AgentInstallSection takes `platforms` prop — pass `tool.platforms` instead of the global `PLATFORMS` array. But AgentInstallSection might need the full platform list for rendering all possible platform tabs. Check the component's interface.
- If AgentInstallSection needs the full platform list, add a fetch for `/api/platforms` or pass it as an additional prop. Simplest: fetch the platforms list from API or from the tool response.
- Actually, the safest change: Import PLATFORMS from mock-data as a FALLBACK only for AgentInstallSection. The tool.platforms already has the specific platforms. For AgentInstallSection which renders install guide tabs, it iterates the installGuide keys, not PLATFORMS. So it should work with just the tool data.

**4. `src/app/categories/[slug]/page.tsx`** — Category detail (server component):
- Remove imports: `CATEGORIES`, `getTools` from mock-data
- Keep `generateStaticParams` using mock-data import (build-time only)
- In page body: fetch `/api/categories/${slug}` — response includes `{ data: { ...category, tools: [...] } }`
- Use the API response for both category info and tools list
- Remove the manual serialization code — API response is already serializable JSON

**5. `src/app/rankings/page.tsx`** — Rankings (client component):
- Remove import: `TOOLS` from mock-data
- Add `useState`, `useEffect` for fetching
- In useEffect, fetch `/api/tools?limit=100` to get all tools for ranking
- The tab-based sorting (overall/weekly/newest) can stay client-side since the data is already fetched — no need for separate API calls per tab
- Alternative: Fetch from `/api/tools?sort=stars`, `/api/tools?sort=recent`, `/api/tools?sort=name` per tab. But this adds latency. Better to fetch once and sort client-side.
  </action>
  <verify>
    <automated>grep -c "from.*mock-data" src/app/page.tsx src/app/tools/\[slug\]/page.tsx src/app/categories/\[slug\]/page.tsx src/app/rankings/page.tsx src/app/tools/\[slug\]/ToolDetailClient.tsx 2>/dev/null | awk -F: '{if($2>0) print "STILL USES MOCK: "$1; else print "OK: "$1}'</automated>
  </verify>
  <done>
    - Home page fetches featured/newest tools and categories from API
    - Tool detail page fetches tool by slug from API
    - Category page fetches category with tools from API
    - Rankings page fetches tools from API and sorts client-side
    - ToolDetailClient no longer imports PLATFORMS from mock-data (or only as fallback)
    - No remaining imports from mock-data in page rendering code (except generateStaticParams which is build-time)
    - All pages handle loading states and errors gracefully
    - All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tools list and FilterBar to use API with live filtering</name>
  <files>src/app/tools/ToolsClient.tsx, src/components/ui/FilterBar.tsx</files>
  <read_first>
    - src/app/tools/ToolsClient.tsx (current: imports getTools from mock-data, uses useMemo for client-side filtering)
    - src/components/ui/FilterBar.tsx (current: imports CATEGORIES/PLATFORMS from mock-data for dropdowns)
    - src/lib/api-utils.ts (API response shape reference)
  </read_first>
  <action>
**1. `src/components/ui/FilterBar.tsx`** — Update to fetch categories and platforms from API:
- Remove imports: `CATEGORIES as ALL_CATEGORIES, PLATFORMS as ALL_PLATFORMS` from mock-data
- Add `useState`, `useEffect` for data fetching
- Add state: `categories`, `platforms`
- In useEffect, fetch `/api/categories` and use the response data for the category dropdown
- For platforms: There is no `/api/platforms` endpoint (not in requirements). Options:
  a) Keep importing PLATFORMS from mock-data (simplest, platforms rarely change)
  b) Derive platform list from tool data
  Choose option (a): Keep PLATFORMS import from mock-data for the platform dropdown. Platforms change very rarely and don't need API-backed data yet. This avoids scope creep.
- Replace CATEGORIES dropdown with API-fetched categories (with tool counts)
- Keep all other FilterBar logic the same (type toggles, sort dropdown, filter change handler)

**2. `src/app/tools/ToolsClient.tsx`** — Update to fetch from API:
- Remove import: `getTools` from mock-data
- Add `useState`, `useEffect`, `useCallback` for data fetching
- Add state: `tools` (Tool[]), `loading` (boolean), `meta` (pagination info)
- Create a `fetchTools` function that builds the API URL from current filters:
  ```typescript
  const fetchTools = useCallback(async () => {
    const params = new URLSearchParams();
    if (filters.query) params.set('q', filters.query);
    if (filters.type) params.set('type', filters.type);
    if (filters.platform) params.set('platform', filters.platform);
    if (filters.category) params.set('category', filters.category);
    if (filters.sort) params.set('sort', filters.sort);
    if (filters.page) params.set('page', String(filters.page));
    params.set('limit', '20');

    const res = await fetch(`/api/tools?${params.toString()}`);
    const json = await res.json();
    if (json.success) {
      setTools(json.data);
      setMeta(json.meta);
    }
  }, [filters]);
  ```
- Call `fetchTools` in useEffect whenever filters change (replaces the current `useMemo` pattern)
- Show loading state while fetching (simple spinner or "Loading...")
- Keep the same JSX structure — just use `tools` state instead of `useMemo` result
- The empty state ("No tools found") stays the same
- IMPORTANT: The type filter in the URL uses short form ('mcp', 'skill', 'rule') — the API's buildWhereClause handles the mapping to MCP_SERVER/SKILL/RULE. Verify this works end-to-end.
  </action>
  <verify>
    <automated>grep -c "from.*mock-data" src/app/tools/ToolsClient.tsx src/components/ui/FilterBar.tsx 2>/dev/null | awk -F: '{s+=$2} END {if(s==0) print "OK: no mock-data imports in tools/filterbar"; else print "WARNING: still imports mock-data"}'</automated>
  </verify>
  <done>
    - ToolsClient fetches tools from /api/tools with query params based on filters
    - FilterBar fetches categories from /api/categories for the dropdown
    - FilterBar still uses PLATFORMS from mock-data (acceptable — platforms rarely change)
    - Filtering by type, platform, category, and search query works via API
    - Pagination works (page changes trigger new API fetch)
    - Loading state displayed while fetching
    - Empty state displayed when no results
    - No `getTools` import from mock-data in ToolsClient
    - All existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API routes | Fetch calls from client components to API — standard HTTP boundary |
| server component -> API route | Server components fetching from own API via localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | client-side fetch | accept | Client-side rate limiting not needed for MVP; API pagination limits response size |
| T-03-02 | Information Disclosure | error handling | mitigate | API errors return generic messages; client shows user-friendly error states |
</threat_model>

<verification>
1. `npm run build` succeeds (all pages compile)
2. No mock-data imports in page rendering code (grep check)
3. `npm test` — all existing tests pass
4. Manual: visit / — shows tools and categories from API
5. Manual: visit /tools — shows filterable tool grid
6. Manual: visit /tools/filesystem-mcp — shows tool detail
7. Manual: visit /categories/database — shows category with tools
8. Manual: visit /rankings — shows ranked tool list
</verification>

<success_criteria>
- All 6 pages fetch data from API routes instead of mock-data imports
- FilterBar dropdowns populated from API data (categories) or mock-data (platforms)
- Filtering and sorting work through API query params
- Pages handle loading and error states
- Mock data file preserved as fallback (not deleted)
- All existing tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-03-SUMMARY.md`
</output>
